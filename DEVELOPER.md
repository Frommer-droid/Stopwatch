# 🛠️ Руководство разработчика (DEVELOPER.md)

## 📋 Обзор проекта
**Stopwatch** — это настольное приложение на Python (PySide6) для управления временем. Оно включает в себя таймер обратного отсчета, секундомер и функции автоматизации (клики по координатам).

### Основные возможности:
- **Таймер:** Обратный отсчет с визуальным и звуковым оповещением.
- **Секундомер:** Отсчет времени с возможностью установки будильника.
- **Автоматизация:** Автоматические клики мышью по заданным координатам при старте/остановке таймера.
- **Горячие клавиши:** Управление таймером с клавиатуры (по умолчанию F3 - старт/стоп, F8 - сброс).
- **Трей:** Возможность сворачивания в системный трей.
- **Настройки:** Сохранение позиции окна, длительности таймера и других параметров в `settings.json`.

## 📂 Структура проекта

```
Stopwatch/
├── Stopwatch.pyw          # Главный файл приложения (точка входа)
├── settings.json          # Файл конфигурации (создается автоматически)
├── logo.ico               # Иконка приложения
├── *.mp3                  # Звуковые файлы (Начал.mp3, Закончил.mp3 и др.)
├── get_coords.ahk/.exe    # Утилита для получения координат мыши
├── Build_Tools/           # Инструменты для сборки и релиза
│   ├── project_name.spec.template  # Шаблон для PyInstaller
│   ├── post_build_template.py      # Шаблон пост-билдинга
│   ├── 00_CrRel_template.pyw       # Шаблон создания релиза
│   └── 00_Move_template.pyw        # Шаблон перемещения
├── VERSION                # Файл с текущей версией (будет создан)
├── RELEASE_NOTES.md       # История изменений
├── README.md              # Описание проекта
└── *_GUIDE.md             # Гайды по настройке
```

## 🚀 Запуск и разработка

### Требования
- Python 3.10+
- Зависимости: `PySide6`, `keyboard`, `pyautogui`, `pygetwindow`, `pygame`, `pyperclip`

### Установка зависимостей
```bash
pip install PySide6 keyboard pyautogui pygetwindow pygame pyperclip
```

### Запуск из исходного кода
```bash
python Stopwatch.pyw
```

## 📦 Сборка и Релиз

Процесс сборки и релиза автоматизирован с помощью скриптов в папке `Build_Tools` (требует настройки).

1. **Версионирование:**
   - Версия хранится в файле `VERSION` в корне проекта.
   - При сборке версия "запекается" в `app/version.py` (нужно создать).

2. **Сборка (Build):**
   - Используется `PyInstaller`.
   - Запуск через `Build_Tools/SpecCompiler.pyw` (или аналог).
   - После сборки запускается `post_build.py` для очистки и копирования ресурсов.

3. **Релиз:**
   - Скрипт `00_CrRel.pyw` упаковывает готовую сборку в архив с версией.
   - Скрипт `00_Move.pyw` обновляет установленную версию в папке `Portable Soft`.

## 📝 Правила разработки
- **Кодировка:** Все файлы должны быть в UTF-8.
- **Пути:** Используйте `resource_path()` для доступа к файлам.
  - **Приоритет поиска:**
    1. **Рядом с .exe** (для возможности замены пользователем конфигов и звуков).
    2. **Внутри .exe** (папка `_internal`, дефолтные ресурсы).
    3. **Папка исходников** (для dev-режима).
- **Абсолютные пути:** При работе с файловой системой используйте абсолютные пути, вычисляемые относительно `sys.executable` (для exe) или `__file__` (для скрипта).

---
*Документ создан автоматически.*
